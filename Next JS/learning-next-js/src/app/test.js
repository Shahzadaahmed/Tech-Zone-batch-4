const objA = {
    item: "potato",
    type: "vegetable"
};

const arr = ['a', 'b', 'c', 'd', 'e'];

const names = ['john', 'andrew', 'nick', 'mark', 'douglas'];

// export default objA; Note" For exporting 1 item...!

export { objA, arr, names };